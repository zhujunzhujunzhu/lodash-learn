
import { drop } from "lodash-es";
{ // 使用
  /**
    官方文档: https://www.lodashjs.com/docs/lodash.drop
    
    _.drop([1, 2, 3]);// => [2, 3] _.drop([1, 2, 3], 2);// => [3] _.drop([1, 2, 3], 5);// => [] _.drop([1, 2, 3], 0);// => [1, 2, 3]

   */  

}

{
  // 自定义实现
}

{
  // 源码学习
   
}
