
import { takeRight } from "lodash-es";
{ // 使用
  /**
    官方文档: https://www.lodashjs.com/docs/lodash.takeRight
    
    _.takeRight([1, 2, 3]);// => [3] _.takeRight([1, 2, 3], 2);// => [2, 3] _.takeRight([1, 2, 3], 5);// => [1, 2, 3] _.takeRight([1, 2, 3], 0);// => []

   */  

}

{
  // 自定义实现
}

{
  // 源码学习
   
}
